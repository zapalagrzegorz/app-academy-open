<% if model.errors.any? %>
  <aside class="grid-container">

    <div class="callout alert">
      <h2 class="black"><%= pluralize(model.errors.count, "error") %> prohibited this <%= model.class %> from being saved:</h2>

      <ul>
        <% model.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  </aside>
<% end %>
