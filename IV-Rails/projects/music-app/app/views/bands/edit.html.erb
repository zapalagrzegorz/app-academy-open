<%= render 'form_errors', model: @cat %>

<section class="grid-container form__container">
  <div class="grid-x">
    <div class="cell small-12">
      <h1 class="form__header">Edit kitty: <%= @cat.name %></h1>
    </div>

    <div class="cell small-12">
      <%= render 'form', cat: @cat %>
    </div>
  </div>
</section>

<% content_for :footer do %>
  <p> Cats page</p>
<% end%>
