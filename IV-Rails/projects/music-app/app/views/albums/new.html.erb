<%= render 'form_errors', model: @album %>

<section class="grid-container form__container">
  <div class="grid-x">
    <div class="cell small-12">
      <h1 class="form__header">Create a new band's album!</h1>
    </div>
    <div class="cell small-12">
      <%= render 'form', bands: @bands, album: @album  %>
    </div>
  </div>
</section>

<% content_for :footer do %>
  <p> New album page</p>
<% end %>
